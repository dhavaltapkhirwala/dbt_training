version: 2

models:
  - name: fct_orders
    columns:
      - name: order_id
        tests:
          - unique
          - not_null

  - name: customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
          - relationships:
              field: customer_id
              to: ref('stg_customers')